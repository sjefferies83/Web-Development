<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <% include templates/header.ejs %>
    <p>Welcome to <%= title %></p>
    <!accesses the local variable json file is stored in and pulls out main category name!>
    <h3><%= videodata.categoryName %></h3>
  <ul>
    <!accesses the local variable json file is stored in and loops through the data and displays each category name!>
    <% videodata.categories.forEach(function(item){%>
      <li><a href="https://www.thenewboston.com/videos_food.php?cat=<%= item.categoryID %>"><%= item.categoryName %></a></li>
    <% });%>
  </ul>
  </body>
</html>
